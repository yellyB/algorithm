class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        maxCnt = 0
        for sen in sentences:
            cnt = len(sen.split(' '))
            maxCnt = max(maxCnt, cnt)
            
        return maxCnt
